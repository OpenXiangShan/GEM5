# 执行计划规范（ExecPlan）

本文档参考 [OpenAI 的 ExecPlan](https://github.com/openai/exec-plan) 规范，并结合实际项目需求进行了修改。

本文档定义仓库中的执行计划（ExecPlan）应该如何编写和维护。

ExecPlan 用于解决这类任务：

- 不能一两次对话就做完的任务
- 需要跨多个步骤、多个文件、多个实验推进的任务
- 需要记录中间结论、证据、决策和当前进度的任务
- 容易因为上下文过长而偏离目标的任务

如果任务只是一个很小的改动、简单 bug 修复、单文件调整，一般不需要单独写 ExecPlan。

## 1. ExecPlan 是什么

ExecPlan 不是随手写的 TODO，也不是简单任务清单。

它是一个**面向执行的活文档**，用于回答下面这些问题：

- 这个任务到底要解决什么问题
- 为什么要做
- 当前已经知道什么
- 下一步具体做什么
- 如何判断做对了
- 过程中发现了什么新情况
- 为什么中途改变方案

一个合格的 ExecPlan，应当让一个**不了解上下文的人**，只看这份文档，也能大致接手继续推进。


## 2. 什么时候应该使用 ExecPlan

遇到下面情况时，应优先创建或更新 ExecPlan：

1. 任务预计需要较长时间，或需要跨多轮会话推进
2. 任务包含研究、实验、排查、实现中的两项及以上
3. 任务涉及多个模块、多个文件或多个证据来源
4. 任务中存在不确定性，需要先验证假设
5. 任务需要记录“为什么这样做”，而不只是“改了什么”
6. 任务可能中途暂停，之后还要继续

典型例子：

- gem5 与 RTL 行为对齐
- 前端/BPU/FTQ/flush/redirect 相关问题排查
- 性能回退分析
- 较大的重构
- 需要分阶段落地的新功能
- 需要先做 prototype 才能决定正式方案的任务


## 3. 编写原则

### 3.1 自包含

ExecPlan 必须尽量自包含。  
不要假设读者记得之前的聊天记录，也不要只写“和前面讨论一致”。

如果某个背景对任务推进是必要的，就直接写进当前文档。

### 3.2 面向结果

不要只写“修改某个函数”或“增加某个字段”。  
要说明：

- 预期实现什么效果
- 用户或开发者最终能观察到什么变化
- 如何验证结果

### 3.3 面向证据

尤其是分析类任务，不能只写猜测。  
计划中应明确区分：

- 已确认事实
- 当前假设
- 待验证问题
- 支撑结论的证据

### 3.4 持续更新

ExecPlan 是活文档，不是一次性文档。  
每推进一段，就应更新：

- 当前进度
- 新发现
- 决策变化
- 下一步动作

### 3.5 优先写清楚“为什么”

实现细节可以逐步展开，但关键决策必须说明原因。  
后续接手的人应该能看懂：为什么不用别的方案，而采用当前方案。


## 4. 推荐存放方式

建议每个复杂任务单独一个文件，而不是把所有任务写到同一个大文档中。

推荐目录结构：

```text
docs/
  exec-plans/
    active/
    completed/
    blocked/
````

说明：

* `active/`：正在推进中的任务
* `completed/`：已经完成的任务
* `blocked/`：暂时卡住、等待外部条件的任务

文件名建议简洁明确，例如：

* `docs/exec-plans/active/gem5-rtl-fetch-align.md`
* `docs/exec-plans/active/bpu-override-investigation.md`
* `docs/exec-plans/active/spec06-regression-debug.md`


## 5. 推荐结构

每个 ExecPlan 建议至少包含以下部分。

## 标题

用一句简短的话描述任务目标。
标题应尽量是“动作 + 对象”，而不是模糊命名。

例如：

* 对齐 gem5 与 RTL 的前端 flush 行为
* 排查 SPEC06 某用例 IPC 回退原因
* 为 BPU override 增加可验证统计信息

---

## 背景与目标

用几段话写清楚：

* 当前问题是什么
* 为什么值得做
* 做完后应该达到什么结果
* 结果如何被观察到

这里优先写“任务意义”和“最终效果”，不要一开始就陷入实现细节。

---

## 当前已知信息

写明目前已经确认的背景、现象和限制条件。
可以包括：

* 涉及的模块、文件、路径
* 当前行为与预期行为的差异
* 已经观察到的日志、计数器、trace、波形、测试结果
* 当前环境限制

这里不要把“猜测”写成“事实”。

## 假设与待验证问题

如果任务还有不确定性，应单独列出，例如：

* 当前怀疑问题出在 override 生效时机
* 当前不确定 second target 是否由 mainBTB 提供
* 需要验证某个 counter 是否覆盖 split request 场景

这部分的目的是防止分析过程越来越乱。

---

## 计划步骤

按推进顺序写清楚下一步打算做什么。
每一步尽量写成“动作 + 目标 + 产出”的形式。

例如：

1. 阅读前端 redirect 相关路径，确认 gem5 中实际生效的控制流
2. 对 RTL 文档和实现交叉比对，整理 flush 分类口径
3. 增加必要日志或统计，构造最小复现场景
4. 用指定 workload 验证差异是否收敛
5. 根据验证结果决定保留当前方案还是调整方案

不要写成只有自己才看得懂的缩写列表。

---

## 验证方式

必须写清楚怎么判断任务成功。
验证可以是：

* 测试通过
* 日志符合预期
* 统计值变化符合预期
* 某个 workload 的行为与 RTL 一致
* 某个性能回退被消除
* 某个场景可稳定复现并被修复

如果只是分析任务，也应给出“分析完成”的判定标准，例如：

* 已确认根因
* 已给出最小复现实验
* 已排除若干候选原因
* 已形成下一阶段实施建议

## 进度

进度必须持续更新。
建议使用复选框，并带时间戳。

示例：

* [x] 2026-03-24 10:00 阅读 gem5 前端 redirect 主路径，确认主要入口函数
* [x] 2026-03-24 11:20 对照 RTL 文档，发现 flush 分类口径与原先理解不一致
* [ ] 为 split request 场景补充统计，确认 inflightLoads 是否完整覆盖
* [ ] 构造最小 workload 验证 second target 选择逻辑

如果某一步做了一半，要明确写清“已完成什么，剩下什么”。

---

## 发现与意外情况

记录推进过程中出现的关键新发现。
尤其是这些内容：

* 原先理解有误
* 文档与代码不一致
* 某个统计口径不可靠
* 某条路径比预期更关键
* 某个实验结果推翻了之前的假设

这部分很重要，因为长任务最怕“中途学到的新信息没有沉淀下来”。

---

## 决策记录

只要发生了关键决策或改方向，就记录下来。

格式建议：

* 决策：……
* 原因：……
* 日期：……

例如：

* 决策：先补充观测点再修改行为逻辑
* 原因：当前根因尚未完全确认，直接改行为风险过高
* 日期：2026-03-24

---

## 结果总结 / 回顾

在阶段性完成或整体完成后，补一段总结：

* 最终完成了什么
* 哪些目标达成了，哪些没有
* 还有哪些遗留问题
* 这次任务中学到了什么
* 后续最自然的下一步是什么

---

## 6. 对分析类任务的额外要求

对于行为对齐、性能分析、问题排查类任务，建议额外注意：

### 6.1 区分“现象”“推测”“结论”

不要混写。
推荐显式区分：

* 现象：实际观察到的行为
* 推测：当前认为可能的原因
* 结论：经过证据支持后确认的结果

### 6.2 优先保留关键证据

必要时把关键证据简要贴在文档里，例如：

* 一小段日志
* 一组关键 counter
* 一个最小复现命令
* 一段简短代码路径说明

不要贴很长的大段原始输出，只保留最能证明结论的部分。

### 6.3 先验证，再改实现

当根因不明确时，优先设计观测点、实验和 prototype，
不要一开始就大改代码。

---

## 7. 对实现类任务的额外要求

对于功能开发、重构、接口变更类任务，建议补充：

* 会修改哪些文件
* 会新增哪些接口或类型
* 兼容性风险是什么
* 如何逐步验证而不是一次性大改
* 是否需要分阶段落地

如果需要 prototype 或双路径并存，也要明确说明何时移除旧路径。

---

## 8. 不推荐的写法

以下写法应尽量避免：

* 只写一句“修复某某问题”
* 只有 TODO，没有背景和验证方式
* 只记录最终代码改动，不记录中间判断依据
* 大量依赖“前文已知”“讨论过了”“见聊天记录”
* 把猜测写成结论
* 计划步骤过于抽象，后续无法执行
* 文档长期不更新，实际状态和文档严重脱节

---

## 9. 最低标准

一个合格的 ExecPlan，至少应满足：

1. 看得出任务目标是什么
2. 看得出当前做到哪里了
3. 看得出下一步该做什么
4. 看得出如何验证结果
5. 看得出中途为什么改过方向
6. 即使换一个人继续，也不会完全失去上下文

---

## 10. 最小模板

下面是建议模板，新建 ExecPlan 时可以直接复制：

# <任务标题>

## 背景与目标

说明当前问题、任务价值、目标结果，以及做完后如何观察到效果。

## 当前已知信息

记录当前已经确认的现象、背景、涉及模块、限制条件和已有证据。

## 假设与待验证问题

列出当前仍不确定的问题，以及准备如何验证。

## 计划步骤

1. …
2. …
3. …

## 验证方式

说明如何判断任务成功，包含测试、日志、统计、行为或性能结果。

## 进度

* [ ] ...
* [ ] ...
* [ ] ...

## 发现与意外情况

* …

## 决策记录

* 决策：…
  原因：…
  日期：…

## 结果总结 / 回顾

任务完成或阶段结束后填写。

```
